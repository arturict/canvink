{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal core capability for the Canvink main window. No shell, filesystem, HTTP, or network plugin permissions are granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-destroy"
  ]
}
